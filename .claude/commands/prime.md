---
description: Load foundational context for this codebase
---

# Purpose

Orient yourself in this project — a Pi coding agent codebase with extensions, agent specs, themes, safety rules, and orchestrated workflows.

## Workflow

1. Run `git ls-files --others --cached --exclude-standard` to see the project file tree
2. Read `justfile`, `THEME.md`
3. Read `extensions/*`
4. Read `.pi/agents/*`
5. Read `.pi/settings.json` and `.pi/themes/*`
6. Summarize your understanding of the project: purpose, stack, structure, key files, and entry points
